import React, {Fragment} from "react";

//views
import View from "./view"

const Banner = (props)=>{
    return(
        <Fragment>
            <View/>
        </Fragment>
    );
}

export default Banner;